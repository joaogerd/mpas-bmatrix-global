# Nota final da refatoração BFLOW

A branch atual entrega a primeira etapa da migração para Python puro. Ela não remove completamente o NCL, mas deixa as dependências externas isoladas e removíveis.

O próximo desenvolvimento deve se concentrar apenas em `weights.py` e `psichi.py`.
